# CodexBuddy v1.0.0

v1.0.0 是 CodexBuddy 的首个正式稳定版：围绕 Codex 的官方额度、本地 Token 记录与日常任务决策完成产品收尾。界面保持克制的深色卡片风格，所有用量分析继续只在本机完成。

## 正式版收尾

- 用量详情：恢复独立的累计、今日、本周、本月指标卡；趋势、当前额度、项目与模型明细形成清晰主视图，缓存命中率与活动热力图按需展开
- 当前额度：移除不帮助决策的“来源 / 实时事件”行，保留套餐与上次更新时间，右侧额度卡更紧凑
- 设置：常规、显示、提醒、数据四个分页保持独立、固定尺寸切换，避免页面抖动和无意义留白
- 用量决策：提供每周安全余量、额度耗尽预警、重置倒计时、每周回顾、额度富余提醒与任务前检查
- 自动化：保留全局快捷键、URL Scheme 和 DMG 内置 `codexbuddy` 命令行助手，可直接打开详情、设置、刷新或运行任务前检查
- 数据：本机会话记录支持项目、模型、来源、缓存命中率、趋势与热力图；小于 1000 万 Tokens 的零碎条目不会挤占详情卡片
- 收尾：删除已弃用的摘要组件与旧版本遗留文案，统一发布构建元数据

## 验证

- 核心与打包自动测试通过
- Apple Silicon 与 Intel 独立构建、DMG、ZIP 和 SHA-256 校验通过

---

# CodexBuddy v0.10.1

v0.10.1 只修复设置窗口稳定性问题。

## 修复

- 设置：移除会在页签切换过程中反复触发的动态高度测量与页面滑动动画。常规、显示、提醒、数据四页现在使用固定且紧凑的内容高度，切换只发生一次无动画尺寸变更，不再出现大幅抖动。
- 设置：打开窗口时立即归一到当前页尺寸，旧版保存的窗口尺寸不会再在首次显示后回弹。

## 验证

- 核心与打包自动测试通过
- Apple Silicon 构建与 DMG 校验通过

---

# CodexBuddy v0.10.0

v0.10.0 把 CodexBuddy 从“看用量”推进为“做用量决策”：预算、预测、任务前检查、数据健康和自动化入口形成闭环，同时收紧了详情与设置的布局。

## 主要变化

- 额度规划：新增每周安全余量（不预留 / 10% / 20% / 30%）与任务前检查，根据剩余额度、重置时间、预留和近期消耗趋势，建议开始、拆小任务或保留额度；不会估算或伪造提示词成本
- 自动化：DMG 内置 `codexbuddy` 命令行助手，支持打开详情、设置、刷新与任务前检查；`preflight --copy` 会刷新并把结论输出到终端，适合 Raycast、Alfred 与 Shell
- 数据健康：数据页新增可信度状态，明确区分可信、可能过期、需要关注和等待数据，并显示读取来源与本地样本数
- 详情：项目、模型和来源明细仅保留 ≥ 1000 万 Tokens 的条目；零碎事件仍计入总量、趋势与热力图，但不再挤占卡片空间
- 布局：修复设置窗口隐藏分页互相叠放导致的分页失效；恢复常规、显示、提醒、数据四个独立页签。详情右侧额度卡不再撑出大块无意义空白
- 智能提醒：延续 v0.9 的耗尽预警、重置倒计时、额度富余提醒和每周复盘，仍由一个总开关统一管理，避免提醒泛滥

## 验证

- 核心与打包自动测试通过
- Apple Silicon 构建、DMG 与 SHA-256 校验通过

---

# CodexBuddy v0.9.1

v0.9.1 根治了设置窗口切换分页时的抖动问题。

## 主要变化

- 设置：修复点击分页（如“显示”）时窗口抖动——此前分页滑动与窗口高度动画跑在两个不同的动画时钟上，窗口调整比滑动晚 3~4 帧；现改由同一个 SwiftUI 动画事务逐帧驱动窗口高度，滑动与窗口变高完全同步
- 设置：四个分页高度改为后台预测量，首次切换任何分页动画即从第一帧同步启动，不再等布局回调

## 验证

- 逐帧插桩日志确认修复后窗口高度单调平滑、与切页过渡同事务插值，无回退无跳变
- 截图验证显示页渲染正常，隐藏测量探针不可见
- 核心与打包自动测试通过
- Apple Silicon 与 Intel 独立构建通过

---

# CodexBuddy v0.9.0

v0.9.0 主题是“低频高价值提醒 + 重度用户定制”：新增每周用量复盘与额度富余两类智能通知，菜单栏支持今日 Tokens 模式与迷你趋势线，并带来全局快捷键与 URL Scheme 深链，方便 Raycast / Alfred / 脚本集成。

## 主要变化

- 通知：新增“每周用量复盘”——每周额度重置后推送上周摘要：用掉多少额度、总 Tokens、最烧的项目、较前一周环比，每周期仅一条
- 通知：新增“额度富余提醒”——每周额度重置前 12 小时内若仍剩 50% 以上，提醒你放开用，别让额度白白过期
- 菜单栏：新增“今日 Tokens”显示模式（与 H/W、最紧张额度并列），直接在菜单栏看今天烧了多少 Token，中文万/亿、英文 K/M/B 紧凑格式
- 菜单栏：可选迷你趋势线（sparkline）——百分比模式绘制近 24 小时剩余额度走势，今日 Tokens 模式绘制近 7 天消耗走势，自动适配深浅菜单栏
- 快捷键：新增全局快捷键 ⌥⌘U，在任意应用中一键打开 / 收起用量详情，无需辅助功能权限，可在设置中关闭
- 集成：新增 URL Scheme 深链——`codexbuddy://dashboard` 打开用量详情、`codexbuddy://settings` 打开设置、`codexbuddy://refresh` 立即刷新，方便 Raycast / Alfred / 脚本调用
- 设置：显示页新增迷你趋势线与全局快捷键开关，实时预览支持今日 Tokens 模式
- 新增富余提醒门控、周报统计聚合与紧凑 Token 格式化的自动测试

## 验证

- 核心与打包自动测试通过
- 截图验证菜单栏今日 Tokens 模式、迷你趋势线与设置显示页新增开关
- 实测 ⌥⌘U 全局快捷键与 codexbuddy:// 深链唤起
- Apple Silicon 与 Intel 独立构建通过

---

# CodexBuddy v0.8.0

v0.8.0 主题是“从看额度，到看懂额度”：用量详情新增项目 / 模型 / 来源三大维度的消耗拆分与缓存命中率洞察，并带来智能耗尽预警通知。全部分析基于本地会话日志，数据不出本机。

## 主要变化

- 用量详情：新增“项目用量排行”卡片，按项目维度统计 Token 消耗 Top 5，悬停可查看项目完整路径
- 用量详情：新增“模型与来源”卡片，展示各模型用量占比及 VS Code / CLI / Exec 等使用入口分布
- 用量详情：新增“缓存命中率”卡片（缓存输入 / 输入总量），带较上期环比变化；命中率越高，同样额度能跑更多任务
- 用量详情：三张洞察卡片跟随趋势图的 7/30/90 天范围切换联动刷新
- 通知：新增额度耗尽预警——按最近消耗速度预测额度将在重置前耗尽时提前提醒（5 小时窗口提前 90 分钟内、每周窗口提前 12 小时内），比固定阈值更懂节奏，每个重置周期只提醒一次
- 数据层：会话扫描器升级为多维度聚合（项目/模型/来源/缓存细分），仍为增量扫描；首次升级会自动全量重建一次本地统计缓存
- 新增耗尽预警门控与多维度聚合的自动测试

## 验证

- 核心与打包自动测试通过
- 真实数据下截图验证三张洞察卡片及 7/30/90 天范围联动
- Apple Silicon 与 Intel 独立构建通过

---

# CodexBuddy v0.7.4

v0.7.4 修复官方 Usage 页链接失效，全面升级用量详情面板与设置窗口，并带来一批性能与可靠性改进。

## 主要变化

- 设置：窗口高度随分页内容自适应，切换分页时平滑变高，常规页不再留大片空白
- 设置：常规页新增“关于”卡片（应用图标、版本号、GitHub 仓库、检查更新），数据页去掉重复的检查更新按钮
- 设置：窗口标题跟随语言切换即时更新
- 悬浮组件：背景改为真磨砂玻璃效果（透出窗口后方内容的模糊），不再是实底深色
- 用量详情：Token 活动热力图自适应铺满卡片宽度（按窗口宽度显示尾部周数、格子自动放大），右侧不再留大片空白
- 用量详情：额度无数据时显示明确的“暂无数据”状态，不再与 0% 混淆
- 用量详情：活动热力图新增月份与星期轴标签，未来日期不再显示灰格
- 用量详情：Token 趋势图支持悬停查看单日数值，今日柱高亮显示
- 用量详情：状态行显示相对更新时间，数据过期时变橙色提醒
- 用量详情：“本周”卡片在已知官方重置时间时按官方 7 天周期统计，与右侧每周额度口径一致
- 用量详情：额度卡片展示按近期速度的耗尽预估
- 用量详情：新增“导出 CSV”按钮，可导出每日 Token 数据
- 用量详情：指标计算改为快照缓存，窗口常开时不再随额度刷新反复全量重算
- 修复“官方 Usage”菜单打开 404 页面（OpenAI 已迁移到 chatgpt.com/codex/settings/usage），并把链接收敛到 ProductIdentity 集中管理
- 修复设置窗口每次启动后首次打开都回到屏幕中央、丢失用户上次拖动位置的问题
- 自动更新强制 SHA-256 校验：缺少校验文件时拒绝安装
- 自动更新下载改用系统下载接口，消除逐字节读取的 CPU 开销；SHA-256 计算迁移到 CryptoKit
- 版本比较支持预发布后缀（如 0.8.0-beta.1），预发布不再被当作正式版推送
- 会话目录扫描增加早停剪枝，典型场景下尾部读取次数从 20 次降到 1–2 次
- 热路径日期格式化器改为静态缓存，降低解析与渲染开销
- 通知去重改为每类型单 key，不再在 UserDefaults 中无限累积，并自动清理历史死 key
- 新增版本比较测试；test.sh 改为校验 RELEASE_NOTES 与 version.sh 的版本一致性
- README 功能与架构章节同步到当前版本
- 继续分别提供 Apple Silicon 与 Intel 安装包

## 验证

- 核心与打包自动测试通过
- Apple Silicon 与 Intel 独立构建通过
- DMG、ZIP 和 SHA-256 校验文件已生成

---

# CodexBuddy v0.7.3

v0.7.3 带来新的设置窗口、桌面悬浮用量和详细用量面板，并修复登录时启动与设置页切换体验。

## 主要变化

- 新增独立的毛玻璃设置窗口
- 新增点击展开的桌面悬浮用量组件
- 新增 Codex Token 使用详情面板
- 修复登录时启动开关状态同步，并支持跳转系统登录项设置
- 设置栏目切换保持固定窗口尺寸，避免窗口位置跳动
- 继续分别提供 Apple Silicon 与 Intel 安装包

## 验证

- 核心与打包自动测试通过
- Apple Silicon 与 Intel 独立构建通过
- DMG、ZIP 和 SHA-256 校验文件已生成

---

# CodexBuddy v0.7.2

v0.7.2 调整了 Dual Gauge 应用图标在 macOS 中的视觉尺寸。

## 主要变化

- 使用用户确认的新版 Dual Gauge 图标
- 增加透明安全边距，让图标在 Dock、Finder 和“应用程序”目录中保持合适尺寸
- 保留原始 SVG 作为矢量母版
- 继续分别提供 Apple Silicon 与 Intel 安装包

## 验证

- 核心与打包自动测试通过
- Apple Silicon 与 Intel 独立构建通过
- 两套应用签名、DMG、ZIP 和 SHA-256 校验通过
- 从最终 AppIcon.icns 反向提取 1024px 图标检查通过
